# Generic synthetic example

`dashboard_example.xlsx` is a public, synthetic reference derived from the
layout of a private dashboard template. It contains invented identities and
contact details under the reserved `.invalid` domain.

It exists to test the future sample-discovery workflow. It must not be treated as
production data or as a business-specific contract for the platform.
